/**
 * 참조 구현 샘플 — <b>새 프로젝트는 이 패키지를 통째로 삭제하고 시작한다.</b>
 *
 * <p>공통 계층({@code common})·보안({@code security})만으로는 드러나지 않는 "어떻게 쓰라는 것인지"를
 * 실제 동작하는 코드로 보여주기 위한 예제 모음이다. 업무 도메인이 아니다.
 *
 * <p>삭제 시 함께 지울 것
 * <ul>
 *   <li>{@code resources/mapper/AccountMapper.xml}</li>
 *   <li>{@code resources/db/migration/V2__sample_ledger_schema.sql}</li>
 *   <li>{@code resources/db/migration/V4__sample_ledger_seed.sql}</li>
 *   <li>{@code src/test/java/com/example/demo/sample/}</li>
 * </ul>
 */
package com.example.demo.sample;
